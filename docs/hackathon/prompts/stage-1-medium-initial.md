> Public redacted export: local paths, private log links, and task identifiers are replaced or omitted. This is historical task text, not a new instruction.

# Stage 1: Medium task creation prompt

Prompt text extracted from the Coordinator tool call. Machine paths and task identifiers are redacted; remaining wording is preserved. Recipient task: `<TASK_1>`.

## Exact prompt

Sent: 2026-09-13T18:16:47.087Z

Original Coordinator log line: 17317 (local log not published).

```text
Own STAGE ONE for PCB Loop in this new isolated worktree. User explicitly requests two tasks in parallel. Existing task<TASK_2> now owns STAGE TWO official PCB Golf score minimization on already valid small board. You own creating a realistic MEDIUM-SIZE input and placement/topology→full routing→native evaluation iterations toward a valid fully routed board. Once accepted, medium board can later enter stage two. Main coordinator<TASK_4>.

Context: original comma.ai PCB Golf245part board reached44missing connections, too difficult for current demo. Newly derived15part11net52x38mm two-layer single-channel MOSFET/LED circuit routes to0without placement iteration, even shuffled;6placement trialsalso0, wire length493.61→436.57mm, policies tied. User wants a meaningful middle difficulty: larger/coherent original-board-inspired circuit where placement updates can help routability, not artificial router sabotage or weakening rules. Choose medium size based on actual original schematic/netlist, perhaps several interacting functional blocks; exact count your engineering judgment. Preserve original applicable footprints, netclasses, ERC/DRC/custom/electrical/manufacturing rules. Define external supply/control boundaries explicitly. Keep design honest PCB Golf-inspired input, not full challenge submission.

First deliver openable KiCad schematic+board+project and corresponding isolated JITX input, component/net counts, electrical explanation, rule audit and exact viewer design name so user can inspect. Then baseline full routing. If initial placement trivially routes, disclose; use legitimate geometric challenge, preserving reasonable full routing effort, to evaluate placement-driven feasibility. No guarantee solver finds all feasible routings. Use useful diagnostics missing-net/pad-islands/congestion/affected groups for relative group moves, rotations, spacing changes and explicit net-attached via/layer topology. Outer iteration changes placement/topology, inner runs full-board enabled-layer autorouting then native evaluation. Do not portray routing one extra net as placement improvement. All stages immutable checkpoints with board previews, actions, sourceSHA, params, runtime, native reports, keep/reject and reproducible record. Preserve via identities across parent→preview→final and rules after native saves. Zero requires complete intended connectivity and required physical checks, never just router exit or loss proxy. Show actual convergence, no fabricated wins. Retain viable best; honest failures.

Reuse committed infrastructure read-only from <LOCAL_PATH> (branchcodex/small-pcb-loop, small experiment commit baedc40ba8a1305306ce328b741c7f38a2d40e1e) and <LOCAL_PATH> branchcodex/demo-takeover479bb4e6078b8c0aca48dbf6f22e8f49ee0b7ad4. Bring committed code into YOUR worktree, do not edit their dirty files/runtimes. Original source <LOCAL_PATH> Small input/build scripts experiments/small-loop. Small artifacts .local/small-loop in d991. KiCad10.0.6 <LOCAL_PATH>; pcbnew via bundledPython3.9. Freerouting located /private/tmp/copper-router/freerouting-mount/freerouting.app/Contents/MacOS/freerouting. Prior script handles full route. JITX4.4 shared executable <LOCAL_PATH> but use your OWN isolated project/runtime/designs directories, never touch existing designs state.
Known pitfalls: JITX import succeeds on DIRECTORY containing onePCB; direct projectfile misleading refdes mismatch. JITX emits0spacing unless source constraints restored. Native pcbnew.SaveBoard silently changes project settings; preserve original project ERC/net_settings/board.design_settings around save. Original min trace width0.1016mm vs Default nominal0.2mm; full-width run skip router fanout yielded all0.2mm; don't confuse nominal vs global minima. Full relative libraries needed for meaningful DRC. Existing native audit validates pad partitions and original electrical footprint geometry. JITX viewer small design small_loop.design.SmallLoopInput, do not hijack it.

Deliver stage-one observability (loss+board-state replay, logs, action explanations), W&B/Weave in existing philippe-fdesousa/copper-scar using protected local credentials without leaking. Maintain experiment/eval records and controlled equal-budget policy comparison with declared cost@N/tie rule where testing algorithm changes. Do not invent LLM calls for deterministic proposals. No external submission/public visibility change. Commit/push your reproducible source branch, exclude secrets/bulky runtime data, preserve userdirtycheckouts. Coordinate useful interfaces with stage-two task but share only immutable input/output artifacts and read-only code, no competing writes. Document accepted medium-board handoff manifest and original constraints for later stage-two ingestion.

Immediately report worktree/branch and concrete selected medium circuit plan. Provide early preview. Leave concise commentary on meaningful progress. When finished, blocked, or needing user decision, send concise report to originating Main via send_message_to_thread with useful outcome and whether continuing. Keep pursuing real requested result, not stop at first importer/router error.
```
