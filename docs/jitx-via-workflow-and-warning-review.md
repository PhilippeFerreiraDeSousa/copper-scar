# Via workflow and trial009 warning review

The accepted incomplete board remains009:310 opens,44 physical errors,237 warnings,7 parity,ERC0. It is not a valid or manufacturing-qualified board. Source inputs and native generated state have separate ownership; the agent did not edit, delete, restore or replace anything under `designs/`.

## Verified via gap and source proposal

Fresh read-only `phd.load` returned `board.vias=[]` and two copper layers for surviving002. Evidence: `runs/stage1/via-workflow-diagnostics/live-readonly-load.json`. Installed JITX4.4 viewer `services/vias.ts` uses `phd.via-drop` with selected routable origins, layer, optional instance, and `def` containing an existing definition name. `via-add` and `via-create` also consume existing definition names. `via-update` moves existing instances. No operation for installing a new definition was found in the inspected viewer. Redux via-definition selection only selects definitions already received with the board.

`services/autoroute.ts` invokes `phd.route` for selected pad/via IDs on each active copper layer, with `force=false` and optional configuration. It does not add transition vias. Current harness uses those fields and waits for completed export when physical work is still busy. Adequate all-layer routing has therefore not been established by repeatedly routing002 without vias. Missing routes do not prove a geometric impossibility.

The supported Python source builder in `jitx/run/discover.py` translates the entire design and sends root `des.load`. `DryRunBuilder` packages without connecting to the runtime. An offline project copy contains the complete accepted009 placement source plus one substrate inline `Via`: name `Source compliant through via 0.60-0.30`, start0, stop-1 (through), diameter0.60mm, hole0.30mm, mechanical drill, tented, no via-in-pad. The successful dry build's serialized output contains those exact fields. See `source-with-via.json`, `dry-build-result.json`, and `accepted-input-preservation.json` in the diagnostic directory. The initial CLI dry invocation resolved the installed editable package; the subsequent explicit offline import asserted its source path and independently checked emitted via fields. Only that latter result qualifies the proposal.

[Original-rule source](/Users/philippe/dev/PCBGolf/pcbgolf.kicad_pro) requires0.0762mm annular ring. The installed JLC package standard0.45/0.30mm via has(0.45−0.30)/2=0.075mm, narrowly below the original requirement. JLC's [current capabilities](https://jlcpcb.com/capabilities/Capabilities) separately specify via diameter at least0.10mm larger than drill,0.15mm preferred, and0.20mm preferred minimum drill. Its PTH annular-ring table is a different category; treating its0.15mm multilayer requirement as the minimum for all vias was overly conservative. The proposed0.60/0.30mm via has0.15mm ring and0.30mm diameter excess, satisfying both the original annular requirement and current JLC via dimensions. This is a conservative choice, not proof of complete fabrication/current/SI qualification. Existing profile global0.15 ring remains conservative and does not resize predefined vias.

## Concrete blocker and alternatives

An unchanged same-identity source rebuild007 of006 caused native `FATAL ERROR: Illegal tag bits` during physical layout/cache loading and exit255. Its complete input state was copied outward beforehand. That same supported full-load path is required to register the via source. Reloading the only surviving accepted client is not established to preserve usable state, and a preserved export is not a supported way to restore native routing. Accordingly the source proposal has not been installed in live002 and no blind routing retry was started.

Preferred continuation: obtain a supported native reload repair or documented definition-update operation that preserves the existing physical state. A separate minimal source-only fixture can qualify basic via creation, but cannot prove the245-part cached-state reload safe. An explicitly separate fresh full-board experiment could test routing with vias and the saved009 poses, but would start without retained copper; it must be called a reset experiment, never an incremental continuation. No such experiment was started here. Native snapshots are evidence only and must not be copied back into `designs/`.

## Warning delta and next placement evidence

`iteration-009-power-neighborhood/warning-review.json` compares original-rule reports by warning type and item descriptions, independent of UUID and global translation. All13 added warnings involve LED9 and C51:3 silk overlaps,10 silkscreen clipped by mask. Existing199 footprint-library mismatches,9 text-height findings, and16 other silk overlaps are unchanged in count. These new warnings do not indicate an added copper short or clearance violation, but polarity/outline readability and body/courtyard separation remain meaningful assembly gates. They are not waived as harmless.

Policy v3 retained009 because opens decreased313→310 while physical errors44, incorrect connections0 and ERC errors0 did not regress. Warnings are lower-priority diagnostic cost, not an acceptance gate waiver. The local placement has a concrete next correction: separate the LED9/C51 outlines and polarity mark while preserving the connected U16 support neighborhood and fixed connector. Before broader placement changes, compare the affected nets' island/pad/layer availability with native routing feedback. Once compliant vias are usable, test a local clearance/spacing expansion separately from relative component arrangement, record comparable bounded effort, and preserve all unaffected poses/copper. Do not infer that an all-board size increase is needed solely from present no-via failures.
